# # ¬ы будете использовать python:3.10-alpine в качестве базового образа дл€ создани€ контейнера Flask
FROM python:3.10-alpine

# ¬ нем указываетс€ рабочий каталог, в котором будет запускатьс€ контейнер Docker
WORKDIR /app

#  опируем все файлы приложени€ в рабочий каталог
COPY . .

# ”становите все зависимости, необходимые дл€ запуска приложени€ Flask
RUN pip install -r requirements.txt

# ќткройте контейнер Docker, чтобы приложение запускалось на порту 5000
EXPOSE 5000

# T оманда, необходима€ дл€ запуска приложени€ с докеризацией
CMD ["python", "/app/app.py"]